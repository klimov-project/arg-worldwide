{
    "buildCommand": "cd frontend && npm install && npm run build",
    "outputDirectory": "frontend/.output/public",
    "devCommand": "cd frontend && npm install && npm run dev",
    "env": {
        "NODE_ENV": "production",
        "FRONTEND_URL": "https://arg-worldwide.vercel.app"
    }
}